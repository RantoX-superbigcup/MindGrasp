# GraspNet Weights

Place the official GraspNet baseline checkpoint here before running real inference.

Expected default path:

```text
weights/graspnet/checkpoint-rs.tar
```

The checkpoint is intentionally not committed to Git because model weights are large binary artifacts. Keep the file local, or distribute it through GitHub Releases / cloud storage / Git LFS if your team needs shared downloads.